<h1><%= @program.title %></h1>
<p>Date: <strong><%= @program.date.strftime('%a %b %d, %Y') %></strong></p>
<p>Time: <strong><%= @program.time.strftime('%l:%M %p') %></strong></p>
<% if @program.category %>
  <p>Category: <strong><%= @program.category.name %></strong></p>
<% end %>
<p>Description: <strong><%= @program.description %></strong></p>
<% if @program.hosts.any? %>
  <p>Hosts:</p>
  <ul><% @program.hosts.each do |host| %>
    <li><%= link_to host.user.email, user_path(host.user_id) %></li>
  <% end %>
  </ul>
<% end %> 

<% if current_user && (current_user.admin? || current_user.staff? || current_user.programs.include?(@program) ) %>
  <%= link_to 'Edit', edit_program_path %>
  <%= link_to 'Delete', program_path, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
